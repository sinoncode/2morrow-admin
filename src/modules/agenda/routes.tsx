import { lazy } from "react";
import { RouteObject } from "react-router-dom";

// const AgendaPage = lazy(() => import("./pages/AgendaPage"));

export const agendaRoutes: RouteObject[] = [
  {
    path: "agenda",
    // element: <AgendaPage />,
  }
];
